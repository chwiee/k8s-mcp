apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp
  labels:
    app.kubernetes.io/name: k8s-mcp
spec:
  selector:
    app.kubernetes.io/name: k8s-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
